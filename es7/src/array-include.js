// Array include method

let numbers = [1, 2, 3, 7, 8]
let find = 7

if (numbers.includes(find)) {
  console.log(`Si se encuentra el valor ${find}`)
} else {
  console.log(`No se encuentra el valor ${find}`)
}
